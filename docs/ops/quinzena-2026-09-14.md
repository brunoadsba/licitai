# Quinzena qualidade — 2026-09-14

Baseline estrutural Art. 6º (heurística PDF, **sem LLM**), conforme [piloto-qualidade.md](piloto-qualidade.md).

## Ambiente

- Stack: `./scripts/up.sh --e2e` OK (Camada 0)
- Corpus: RILC completo ingerido + embeddings `599/599` (pending = 0)
- Script: `PYTHONPATH=backend python backend/scripts/score_art6_fixtures.py --dir fixtures/trs-codeba/piloto-unico`

## Seleção (5 TRs)

Variedade; Emergência fora desta rodada (anonimização pendente).

| TR | `art6_coverage` | Alíneas / gaps (heurística) | Notas |
|----|-----------------|----------------------------|--------|
| `01-agua-mineral` | 70% | fundamentacao, descricao_solucao, adequacao_orcamentaria | GAP |
| `05-concurso-guarda-portuario` | 40% | fundamentacao, descricao_solucao, requisitos, modelo_gestao, selecao_fornecedor, adequacao_orcamentaria | GAP |
| `07-obra-portaria-salvador` | 90% | descricao_solucao | OK (≥90%) |
| `09-ti-pabx-nuvem` | 100% | — | OK |
| `11-compra-epi-epc` | 80% | descricao_solucao, adequacao_orcamentaria | GAP |

**Média dos 5:** 76% · **≥90%:** 2/5

## Sessão ouro LLM (2026-09-14) — `09-ti-pabx-nuvem`

Única análise economic da leva (não rodar os 5 em lote). Orçamento `ANALYSIS_MAX_LLM_CALLS=24` (~12 itens); sem isso o PDF explode em 288 itens e estoura TPM free tier.

| Campo | Valor |
|-------|--------|
| `document_id` | `df1066ae-f5f5-40da-ba2f-5a6426cf8ec6` |
| `analysis_id` | `6f673b1b-bcf5-41fc-8a94-3dbf2a635109` |
| Status | `completed_with_errors` (orçamento + cobertura incompleta de agentes/itens) |
| `art6_coverage` | 100% |
| Itens analisados | 12 / 12 (recorte orçado) |
| Tempo upload→fim | ~13 min (778 s), com 429 TPM Groq + 503 Gemini intermitentes |
| Correções | 77 (muitas Art. 6º “ausência” no recorte inicial — falso positivo vs cobertura 100%) |
| Revisão humana alto/crítico pendente | 10 revisadas: **1 aprovada**, **9 rejeitadas** → rejeição **90%** nesta amostra |
| Pacote SEI | Sim (`GET .../sei-pack` → 12 entradas, ~5,8 kB texto) |
| DOCX | Sim (`GET .../corrected-docx` → ~53 KB) |
| Export colado no SEI real | Pendente (Bruno / elaborador) |

Smoke pré-sessão: `LLM_SMOKE_REAL=1 ./scripts/smoke_llm.sh` OK (após fix `system_prompt`/`user_prompt` em [`scripts/smoke_llm.sh`](../../scripts/smoke_llm.sh)).

## A medir (humano / gate)

| Métrica | Status |
|---------|--------|
| Rejeição humana em alto/crítico | **90%** na sessão ouro (9/10); baseline semana 1 do gate |
| Uso pacote SEI / HTML / DOCX | **Sim** (SEI + DOCX gerados na sessão ouro) |
| `completed_with_errors` | **Sim** (1/1 nesta sessão — esperado com orçamento) |
| Export colado no SEI real | A medir |

## Referência do lote completo (12 piloto-unico)

Média heurística do diretório: **71%** · ≥90%: **2/12** (inalterado vs baseline docs).

## Próximo

1. Quando Solange enviar TRs: repetir score + 1 análise economic por objeto novo (manter `ANALYSIS_MAX_LLM_CALLS=24` no free tier).
2. Colar pacote SEI da sessão ouro no SEI real e anotar se bastou.
3. Anonimizar Emergência antes de cloud.
4. Demais TRs da quinzena só com cota sobrando noutro dia: `07` → `11` → `01` → `05`.
