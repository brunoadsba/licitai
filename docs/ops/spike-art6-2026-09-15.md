# Spike Art.6 coverage — relatório (15/09/2026, SEM código, SEM merge)

> Branch marcador: `spike/art6-coverage-15-09` (aponta para `main`; **não mergear até 28/09**).
> Baseline golden (FakeLLM, código atual): `test_golden.py` + `test_art6_coverage.py` +
> `test_feedback_golden.py` → **13 passed** (precision ≥ 0.88 mantida).

## Achado das Fases 1–2

- Heurística piloto: média 71%, ≥90% em 2/12; gaps sistêmicos `descricao_solucao` (11/12)
  e `adequacao_orcamentaria` (10/12) — são **curadoria dos TRs**, não do validador.
- Análise LLM real do 09 deu `art6_coverage` 100%: a régua atual já acerta quando o TR presta.

## Propostas (pós-gate, nesta ordem)

1. **Fallback contextual no `/gerar-tr`**: quando faltar `descricao_solucao`/`adequacao_orcamentaria`,
   sugerir texto-base a partir das seções existentes (só no gerador, sem tocar na régua de medição).
2. **Prompt do agente estrutural**: pedir citação da seção-fonte por alínea (reduz `uncertain`).
3. **Validador heading-first**: normalizar títulos antes do match (ex: "4 — SOLUÇÃO" vs "solução como um todo").

## Critério de aceite do spike (quando implementar)

- Golden FakeLLM continua 13 passed com precision ≥ 0.88.
- Heurística piloto sobe sem cair nenhum TR que hoje está OK (07, 09).
- Só então propor merge pós-gate.
