# Scripts Windows (PowerShell)

Atalhos para execução **nativa** no Windows (sem Docker).

No piloto WSL/Compose, preferir na raiz do repo:

```bash
./scripts/up.sh
./scripts/down.sh
```

| Script | Uso |
|--------|-----|
| `start_backend.ps1` | Sobe uvicorn no Windows com `.venv` |
